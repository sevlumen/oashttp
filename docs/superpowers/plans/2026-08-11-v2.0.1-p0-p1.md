# v2.0.1 P0-P1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deliver an additive v2.0.1 candidate with corrected support policy, raw `http.Handler` integration, scoped middleware, request-scoped observability metadata, and named security providers.

**Architecture:** Extend the existing operation definition/compiler instead of introducing a second router. Typed and raw operations share registration, OpenAPI metadata, security, request state, and scoped middleware; typed operations retain compiled binding/validation while raw operations retain direct `*http.Request` ownership. Legacy bearer authentication remains backward-compatible rather than being replaced.

**Tech Stack:** Go 1.22+, standard `net/http`, existing internal OpenAPI 3.1 model, GitHub Actions CI.

## Global Constraints

- No third-party runtime dependencies.
- Preserve existing v2 public behavior and legacy bearer/OpenAPI defaults.
- New APIs are additive.
- OAuth2/OpenID Connect flows and first-class scopes remain out of scope for v2.0.1.
- Raw handlers must not cause the core library to parse or buffer streaming bodies.
- Follow RED/GREEN testing for behavior changes.

---

### Task 1: Correct security and support policy

**Files:**
- Modify: `SECURITY.md`
- Modify: `SUPPORT.md`
- Test: `policy_test.go`

**Interfaces:**
- Produces documentation stating `v2.x` is fully supported and `v1.x` receives security fixes only.
- Applies the compatibility promise to major version 2.

- [ ] Add regression tests that require the v2/v1 support language.
- [ ] Verify the tests fail on the original content.
- [ ] Update `SECURITY.md` and `SUPPORT.md`.
- [ ] Verify the tests pass.

### Task 2: Add request-scoped operation and principal metadata

**Files:**
- Create: `operation_context.go`
- Create: `operation_carrier.go`
- Create: `internal/core/operation.go`
- Modify: `internal/security/context.go`
- Modify: `internal/operation/compile.go`
- Modify: `app.go`
- Test: `p1_regression_test.go`
- Test: `p1_principal_metadata_test.go`
- Test: `p1_recovery_metadata_test.go`

**Interfaces:**
- Produces: `type OperationInfo struct { ID, Method, Route string }`
- Produces: `OperationFromContext(context.Context) (OperationInfo, bool)`
- Produces: `OperationID(context.Context) string`
- Produces: `RoutePattern(context.Context) string`
- Preserves: `PrincipalFromContext(context.Context)` while making the principal visible to outer middleware after `next` returns.

- [ ] Write tests asserting typed handlers and scoped middleware can read operation metadata.
- [ ] Write tests asserting application-wide middleware can read operation metadata and principal after routing returns.
- [ ] Write a test asserting `ErrorHandler` sees operation metadata for a recovered routed panic.
- [ ] Install a race-safe request carrier outside recovery/application middleware.
- [ ] Record operation metadata before authentication and principal after authentication.
- [ ] Verify tests and race detection pass.

### Task 3: Add scoped middleware

**Files:**
- Modify: `group.go`
- Modify: `map.go`
- Modify: `operation.go`
- Modify: `internal/operation/definition.go`
- Modify: `internal/operation/compile.go`
- Test: `p1_test.go`

**Interfaces:**
- Produces: `func (g *Group) Use(Middleware) error`
- Produces: `func (b *OperationBuilder[T]) Use(...Middleware) *OperationBuilder[T]`

- [ ] Add tests for parent/child inheritance, registration snapshot semantics, operation middleware, execution order, nil middleware rejection, and frozen app rejection.
- [ ] Store the group middleware snapshot on each operation definition.
- [ ] Wrap scoped middleware after security and before binder/raw input validation.
- [ ] Verify tests pass.

### Task 4: Add generic named security providers

**Files:**
- Modify: `config.go`
- Modify: `security.go`
- Modify: `internal/core/security.go`
- Modify: `internal/security/middleware.go`
- Modify: `internal/operation/definition.go`
- Modify: `internal/operation/compile.go`
- Modify: `app.go`
- Test: `p1_test.go`

**Interfaces:**
- Produces: `type SecurityScheme struct { Type, Scheme, BearerFormat, Name, In string }`
- Produces: `type SecurityProvider interface { SecurityScheme() SecurityScheme; Authenticate(context.Context, *http.Request) (*Principal, error) }`
- Produces: `Config.SecurityProviders map[string]SecurityProvider`
- Produces: `RequireSecurity(name string)` on typed and raw builders.
- Reserves: `bearerAuth` for legacy `Config.Authenticator`.

- [ ] Add tests for named provider authentication from a non-Authorization header and generated OpenAPI scheme references.
- [ ] Preserve legacy `bearerAuth` challenges and output.
- [ ] Validate named OpenAPI `http` and `apiKey` scheme metadata during build.
- [ ] Route operation authentication through the selected provider and authorization through the existing feature/permission policy.
- [ ] Verify tests pass.

### Task 5: Add raw `http.Handler` operations

**Files:**
- Create: `raw.go`
- Modify: `internal/operation/definition.go`
- Modify: `internal/operation/compile.go`
- Modify: `app.go`
- Test: `p1_test.go`
- Test: `p1_regression_test.go`
- Test: `p1_raw_openapi_test.go`

**Interfaces:**
- Produces: `MapHandler(group *Group, method, path string, handler http.Handler) *RawOperationBuilder`
- Produces builder methods: `WithOperationID`, `WithTags`, `WithSummary`, `WithDescription`, `Use`, `Consumes`, `RequireSecurity`, `RequireFeatureAndPermission`, `Produces`, `ProducesProblem`, `ProducesResponse`.

- [ ] Add tests for raw body streaming, path registration, OpenAPI operation/response metadata, security, scoped middleware, duplicate-route handling, and frozen mutation.
- [ ] Enforce route constraints such as `{id:uuid}` before the raw user handler.
- [ ] Add `.Consumes(...)` as OpenAPI request-body metadata only; do not parse or buffer raw bodies.
- [ ] Validate consumed media types at build time.
- [ ] Share routing/security/metadata/middleware while skipping typed JSON binder/validator logic.
- [ ] Verify tests pass.

### Task 6: Documentation and release-candidate verification

**Files:**
- Modify: `README.md`
- Modify: `CHANGELOG.md`

- [ ] Document raw handler escape hatch, `.Consumes`, scoped middleware, request metadata, named security providers, and compatibility boundaries.
- [ ] Add an `Unreleased` v2.0.1 candidate changelog entry without changing `Version` or tagging a release.
- [ ] Run formatting, vet, full tests, race, coverage, golden OpenAPI, fuzz targets, benchmarks, and vulnerability scan through GitHub Actions.
- [ ] Compare branch to `main` and self-review API compatibility and scope.
